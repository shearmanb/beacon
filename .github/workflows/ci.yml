name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        # Version intentionally NOT set here — it's read from package.json's
        # "packageManager" field (single source of truth, shared with Railway /
        # corepack). Setting both errors: ERR_PNPM_BAD_PM_VERSION.
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Typecheck (packages + server + worker + migrate)
        run: pnpm typecheck
      - name: Test
        run: pnpm test
      - name: Web typecheck
        run: pnpm --filter @beacon/web exec tsc --noEmit
      - name: Web build (next build)
        run: pnpm --filter @beacon/web build
