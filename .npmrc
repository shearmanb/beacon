# Lock pnpm's hoisting model. node-linker=isolated is pnpm's current default;
# pinned here so the worker/dashboard `tsx` runtime resolution can't silently
# break if a future pnpm changes defaults or someone adds shamefully-hoist.
node-linker=isolated
